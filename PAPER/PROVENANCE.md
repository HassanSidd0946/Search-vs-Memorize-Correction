# PROVENANCE.md — where every reported number comes from

**Purpose:** a reviewer (or a future editor of this codebase) asking "where does this figure come from" should be able to answer it from this one file — the committed source JSON, the exact field path, and the commit that put the number into the manuscript or fixed how it was rendered. This file is a snapshot as of GATE E (2026-08-28); if a number in `draft.md`/the compiled PDFs ever disagrees with the row below, treat that as a live discrepancy to investigate, not a reason to edit this file to match.

**How to read "source JSON"**: all six run JSONs below live at the repository root and were committed in `a3293792` ("GATE C5: pin BLAS thread determinism, non-overwriting artifacts, commit evidentiary JSONs"), except the R1(b)/R2/R3 rerun, committed separately in `6011c6d1`. These are Modal-execution outputs, not something generated by this session's own commits — this session's commits changed how the manuscript *cites* them, not the underlying data.

---

## R1(b) — encode-vs-test decode

**Source:** `results_r1b_r2_r3_composition_runs_20260825T042232Z_a3293792.json` (the rerun — see "Canonical-source note" below), key `R1b_encode_vs_test`.

| Manuscript figure | Field path | Cited at |
|---|---|---|
| `0.8121 ± 0.0005` (pre-cal balanced) | `.pre_calibration_balanced_accuracy_mean` | draft.md §3a (line 85) |
| `0.8657 ± 0.0005` (post-cal balanced) | `.post_calibration_balanced_accuracy_mean` | Abstract, §3a, Fig. 1 |
| `0.9276` (pre-cal AUC) | `.pre_calibration_auc_mean` | draft.md §3a (line 85) |
| `0.9533` (post-cal AUC) | `.post_calibration_auc_mean` | draft.md §3a (line 85) |
| `0.85` joint-criterion threshold | not a JSON field — pre-registered in DECISIONS.md before this run | Abstract, §3a, Fig. 1 |

Tolerance/AUC-stability derivations: `NUMBERS.md`'s "R2 / R3 / R1(b) — composition runs" table.

## R2 — the corrected contrast

**Source:** same JSON, key `R2_search_vs_memorize_encode_only`; also independently reproduced (bit-identical) in `results_c1_r2_shuffled_label_control.json`'s `real_R2_pre_cal_balanced` and `results_c3_r2_jackknife.json`'s `full_mean_recomputed`.

| Manuscript figure | Field path | Cited at |
|---|---|---|
| `0.5773 ± 0.0005` (pre-cal balanced) | `.pre_calibration_balanced_accuracy_mean` | throughout — Abstract, §2.1/§3.2/§3.4/§3a, all 3 figure captions |
| `0.7311 ± 0.0005` (post-cal balanced) | `.post_calibration_balanced_accuracy_mean` | Abstract, §3.4, §5 items 1/4 |

## R3 — lure-removed contrast

**Source:** same JSON, key `R3_lure_removed`.

| Manuscript figure | Field path | Cited at |
|---|---|---|
| `0.5293 ± 0.0005` (pre-cal balanced) | `.pre_calibration_balanced_accuracy_mean` | draft.md §3a (line 87) |
| `0.7186 ± 0.0005` (post-cal balanced) | `.post_calibration_balanced_accuracy_mean` | draft.md §3a (line 87) |

**Canonical-source note:** the un-timestamped `results_r1b_r2_r3_composition_runs.json` and the `..._20260825T042232Z_a3293792.json` rerun disagree on R2 post-cal, R3 post-cal, R1(b) post-cal AUC, and R3 post-cal AUC. `NUMBERS.md` (STEP 2e) attributes every row above to the rerun under the recency rule; `results/RESULTS_LEDGER.md`'s L039/L052 entries were corrected to match in commit `724d9a73`. All figures in the table above are from the rerun.

## C1 — 30-shuffle null

**Source:** `results_c1_r2_shuffled_label_control.json`.

| Manuscript figure | Field path | Cited at |
|---|---|---|
| `0.4990` (null mean) | `.null_distribution_mean` | §3a, Fig. 2/3 captions |
| `0.0103 ± 0.0001` (null SD) | `.null_distribution_sd` | §3a, Fig. 3 caption |
| real per-shuffle histogram | `.shuffle_level_results[].pre_cal_balanced_accuracy_mean` (30 values) | fig3_null_distribution.py's left panel |

## C4 — 500-shuffle high-resolution null

**Source:** `results_c4_high_res_shuffled_label_control.json`.

| Manuscript figure | Field path | Cited at |
|---|---|---|
| `0.4995` (null mean) | `.null_distribution_mean` | §3a, Fig. 2/3 captions |
| `0.0112` (null SD) | `.null_distribution_sd` | §3a, Fig. 3 caption |
| `p < 0.002` (add-one corrected) | `.p_value_add_one_corrected` | §3a, Fig. 2/3 captions — rendering fixed for IEEE in commit `4979a0fd` |
| real per-shuffle histogram | `.shuffle_level_pre_cal_balanced` (500 values) | fig3_null_distribution.py's right panel |
| `7.78 ± 0.05pp` (R2 vs. C4 null mean) | `R2_REAL − .null_distribution_mean`, computed in fig3_null_distribution.py | Fig. 2 caption, Fig. 3's in-image annotation |

## C3 — leave-one-subject-out jackknife / Table S1

**Source:** `results_c3_r2_jackknife.json`.

| Manuscript figure | Field path | Cited at |
|---|---|---|
| `0.5773 ± 0.0005` (full-sample mean) | `.full_mean_recomputed` | Table S1 caption, Fig. 3 caption |
| `0.5172 ± 0.0005` (null CI upper bound) | `.null_95ci_upper_bound_used` | §3a, Fig. 3 caption |
| `0.5703 ± 0.0005` / `0.5852 ± 0.0005` (LOO min/max, sub-18/sub-03) | `.loo_results[].loo_mean`, found via `min()`/`max()` in fig2_c3_jackknife.py | §3a, Fig. 3 caption |
| `0.0079 ± 0.0005` (largest shift, sub-03) | `.loo_results[].shift_from_full_mean` | §3a |
| `0.0301 ± 0.0005` (half-gap threshold) | derived, `(full_mean − null_CI_upper)/2` | §3a |
| Table S1's 87 cells | `.loo_results[]` × 29 rows × 3 columns (`excluded_fold_pre_cal_balanced`, `loo_mean`, `shift_from_full_mean`) | `supplementary_table_s1.md`, both `.tex` files' embedded table |

Per-column tolerances (`±0.012` / `±0.0005` / `±0.0005`) derived and containment-verified in `NUMBERS.md`'s C3 section and the Table S1 header comment in `build_latex.py`.

## D2 — parity-split replication

**Source:** `results_d2_parity_split_check.json`, keys `groups.odd_search_first` / `groups.even_memorize_first`.

| Manuscript figure | Field path | Cited at |
|---|---|---|
| `0.6157 ± 0.001` (odd real) | `.real_pre_cal_balanced` | §3a, Fig. 4 caption |
| `0.5582 ± 0.001` (even real) | `.real_pre_cal_balanced` | §3a, Fig. 4 caption |
| `0.4987 ± 0.0005` / `0.4985 ± 0.0005` (null means) | `.null_mean` | §3a |
| `0.0109 ± 0.0001` / `0.0147 ± 0.0001` (null SDs) | `.null_sd` | Fig. 4's in-image text only (not in draft.md's own prose) |
| `[0.4793,0.5179] ± 0.0005` / `[0.4724,0.5223] ± 0.0005` (95% CIs) | `.null_percentile_95ci` | §3a |
| `0.1170 ± 0.001` (odd gap above null mean) | `real_pre_cal_balanced − null_mean`, computed in fig4_d2_parity_split.py | §3a, §5 item 3, Fig. 4 caption — tolerance corrected from ±0.0005 in commit `d07b4e9b` |
| `0.0597 ± 0.001` (even gap above null mean) | same, other group | §3a, §5 item 3, Fig. 4 caption |
| real 30-per-subgroup null histograms | `.null_shuffle_values` (30 values each) | fig4_d2_parity_split.py's two panels |

## Effect sizes (two distinct quantities — do not conflate)

| Figure | Formula | Cited at |
|---|---|---|
| `7.73 ± 0.05pp` — R2 vs. the 0.50 chance floor | `(R2_pre_cal − 0.50) × 100` | Abstract, §5 item 4 |
| `7.78 ± 0.05pp` — R2 vs. C4's null mean | `(R2_pre_cal − C4_null_mean) × 100` | Fig. 2 caption, Fig. 3's in-image annotation |

Both derivations and their containment verification: `NUMBERS.md`'s "Effect sizes" section.

## Figure scripts

All four generator scripts (`PAPER/figures/scripts/fig{1,2,3,4}_*.py`) were rewritten to read every value above from its JSON directly, replacing hand-transcribed literals — commit `d07b4e9b`. Regenerated figures committed in `96aabbb8`.

## C4's bit-exactness claim (GATE F STEP 1)

**Source:** the canonical `results_c4_high_res_shuffled_label_control.json` (`consistency_ok=True`, `reproduces_c1_first_30_exactly=True`, `max_abs_diff_vs_c1_first_30=0.0`) vs. its two Aug 27 reruns, `..._20260827T030127Z_a3293792.json` and `..._20260827T030229Z_a3293792.json` (both `consistency_ok=False`, `max_abs_diff_vs_c1_first_30=4.00962...e-4`). draft.md §3a's C4 subsection now scopes its "first 30 of 500 exactly reproduce C1's original 30 values" claim to the run reported here and cross-references §8's non-bit-exactness disclosure, rather than implying the check itself is exactly reproducible.

## Manuscript source and build chain

`PAPER/draft.md` is the single authoritative prose source. `PAPER/figures/scripts/build_latex.py` reads it and the JSONs above and generates `draft_elsevier.tex`/`draft_ieee.tex`/`references_ieee.bib` on every run (full overwrite) — **never edit the `.tex` files directly**, edits will be silently discarded on the next build. Compiled PDFs: commit `26fce664` (GATE F's final recompile, 28pp Elsevier / 11pp IEEE — every page of both visually inspected, not just text-extraction-checked).

## Known cross-file value disagreement (unresolved, flagged not fixed)

`results_r1b_r2_r3_composition_runs.json` (original run) vs. its `_20260825T042232Z_a3293792.json` rerun disagree on four fields (see "Canonical-source note" above). The manuscript uses the rerun throughout. If a future re-run of `run_r1b_r2_r3_composition_runs.py` produces yet a third set of values, treat that as a new disagreement to flag, not a reason to silently prefer the newest file.
